# Documentação interna — não publicar
docs/

# Dependências e build
node_modules/
.next/
out/
.vercel/
*.tsbuildinfo

# Cache bruto da coleta (regenerável) — só o /data normalizado é versionado
/data/raw/

# Misc
*.log
.DS_Store

# Artefatos do brainstorm visual
.superpowers/

# Itemizado federal (nota a nota) — gerado no build/CI (npm run gerar:itemizado), pesado demais p/ o git
/data/despesas/camara-*
/data/despesas/senado-*
